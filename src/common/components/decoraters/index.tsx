import IconArrowRounded from '../decoraters/IconArrowRounded';
import Loading from '../decoraters/Loading';
import Spinner from '../decoraters/Spinner';

export { IconArrowRounded, Spinner, Loading };
